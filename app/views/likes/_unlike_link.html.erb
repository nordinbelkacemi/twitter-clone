<%= link_to(like_path(like), method: :delete, remote: true, class: "unlike-link", id: "unlike-" + like.id.to_s) do %>
    <% if like_count_on_left %>
        <%= like.likeable.likes.count %>
    <% end %>
    <i class='fas fa-heart'></i>
    <% unless like_count_on_left %>
        <%= like.likeable.likes.count %>
    <% end %>
<% end %>